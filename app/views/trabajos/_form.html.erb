<%= form_for([@departamento, @trabajo]) do |f| %>
  <% if @trabajo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trabajo.errors.count, "error") %> prohibited this trabajo from being saved:</h2>

      <ul>
      <% @trabajo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cod %><br>
    <%= f.text_field :cod %>
  </div>
  <div class="field">
    <%= f.label :fecha_bloqueo %><br>
    <%= f.datetime_select :fecha_bloqueo %>
  </div>
  <div class="field">
    <%= f.label :fecha_lib %><br>
    <%= f.datetime_select :fecha_lib %>
  </div>
  <div class="field">
    <%= f.label :hora_estimada %><br>
    <%= f.text_field :hora_estimada %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
